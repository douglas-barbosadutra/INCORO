version: '2'
services:
    usermj-mongodb:
        image: mongo:3.6.3
        ports:
            - "27017:27017"
        command: mongos --configdb usermj-mongodb-config
    usermj-mongodb-node:
        build:
            context: .
            dockerfile: mongodb/MongoDB.Dockerfile
        command: mongod --replSet rs1 --noprealloc --smallfiles
    usermj-mongodb-config:
        image: mongo:3.6.3
        container_name: usermj-mongodb-config
        command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
