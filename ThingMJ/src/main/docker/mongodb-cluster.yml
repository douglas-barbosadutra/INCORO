version: '2'
services:
    thingmj-mongodb:
        image: mongo:3.6.3
        ports:
            - "27017:27017"
        command: mongos --configdb thingmj-mongodb-config
    thingmj-mongodb-node:
        build:
            context: .
            dockerfile: mongodb/MongoDB.Dockerfile
        command: mongod --replSet rs1 --noprealloc --smallfiles
    thingmj-mongodb-config:
        image: mongo:3.6.3
        container_name: thingmj-mongodb-config
        command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
